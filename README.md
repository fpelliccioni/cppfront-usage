# How to use CppFront Conan Recipe/Package

- Install Conan: `pip install conan`

- Clone this repo and `cd` into the cloned directory

- Use `conan` to build the project: `conan create . usage/0.1@`

